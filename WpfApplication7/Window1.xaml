<Window x:Class="WpfApplication7.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication7"
        mc:Ignorable="d"
        Title="Window1" Height="300" Width="300" x:Name="window1">
    <StackPanel>
        <Button Content="改变文本的值" HorizontalAlignment="Left" Click="Button_Click"/>
        
        <!---第一种方式-->
        <!--<TextBlock Text="{Binding ElementName=window1, Path=s.Name}" FontSize="16"/>-->
        
        <!--第二种方式 -->

        <TextBlock Text="{Binding ElementName=window1, Path=Name1}" FontSize="16"/>
    </StackPanel>
</Window>
